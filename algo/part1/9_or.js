/*

Given 2 boolean expressions, "or" returns true or false, corresponding to the || operator.
(boolean 두개가 주어졌을때, "or" 함수는 ||연산자에 대응하는 true 나 false 를반환합니다.)

Note:
    - Do not use the || operator(||연산자를 사용하지 마세요.)
    - Use ! and && operators instead (대신 ! 와 && 연산자를 사용하세요.)
 */

let output = or(true, false);
console.log(ouput); // --> true;

function or(expression1, expression2) {
  // 여기에 코드를 작성하세요
  return;
}
