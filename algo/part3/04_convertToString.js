/*
convertToString 함수가 있습니다. 이 함수는 주어진 파라미터를 문자열의 형태로 반환합니다.

노트:
- 파라미터가 객체이거나, 배열일 경우는 신경쓰지 않아도 좋습니다.
*/

let output = converToString(120);
console.log(output); // --> '120'

let output2 = converToString('hello');
console.log(output2); // --> 'hello'

let output3 = converToString(true);
console.log(output3); // --> 'treu

function converToString(anything) {
  // TODO
  return;
}
