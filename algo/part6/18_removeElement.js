/* 
Write a function called "removeElement"
"removeElement" 함수를 작성하세요.

given an array of elements, and a 'discarder' parameter "removeElement" returns an array 
containing the items in the given array that do not match the 'discarder' parameter

Note
- if all the elements match. It should return an empty array
만약 모든 요소와 일치한다면 ,빈 배열을 반환해야 합니다.
- If an empty array is passed in, it should return an empty array
만약 빈 배열이 줘진다면 , 빈 배열을 반환해야 합니다.
 */

let output = removeElement([1, 2, 3, 2, 1], 2);
console.log(output); // --> [1,3,1]

function removeElement(array, discarder) {
  //TODO
  return;
}
