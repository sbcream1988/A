/*
Using the JavaScript language. have the function ABCheck(str) take the str parameter being passed and
return the true if the characters a and b are separated by exactly 3 places anywhere in the string at
least once Otherwise return the false.
ABCheck(str) 함수를 작성하세요. 문자열(str)이 주어졌을때, ABCheck(str) 주어진 문자열에서 함수는 문자 a와 b 사이가 문자열 안에서
한번이라도 정확이 3글자 떨어져 있으면 true를 반환하고 그렇지 않은 경우는 모두 false를 반환합니다.

For example. "lane Borrowed" would result in true because there is exactly three characters between "a" 뭉 "B"
예를 들어 "lane Borrowed" 문자열은 a 와 B 사이가 정확이 3글자 떨어져 있으므로 true를 반환하게 됩니다.

you should consider about space and capital character
문자 사이의 공백도 한글자로 취급하며, 대문자 A 와 B도 소문자와 동일하게 생각해주셔야 합니다.

Note
 - you have to take care of "undefined" input case
"undefined"를 매개변수로 넣는 케이스도 고려하셔야 합니다.

hint : lowcase , uppercase
*/

function ABCheck(str) {
  //TODO
  return;
}
