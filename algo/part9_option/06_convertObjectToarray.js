/*
Write a function called "convertObjectToList" which converts an objecy literal into an array of arrays, like this
객체를 입력으로 받고 해당 객체를 다음과 같이 2차원 배열로 변형 할 수 있는 함수 "convertObjectToList"를 작성하세요
*/

let input = { name: 'Holly', age: 35, role: 'producer' };
let output = convertObjectToList(input);
console.log(output); // [['name', 'Holly'], ['age', 35], ['role', 'producer']]

function convertObjectToList(obj) {
  // TODO
  return;
}
