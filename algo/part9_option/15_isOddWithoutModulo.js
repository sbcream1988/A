/*
Write a function called "isOddWithotModulo"
"isOddWithotModulo" 함수를 작성하세요

Given a number, "isOddWithotModulo" returns whether the passed in number is odd
숫자가 주어졌을때, "isOddWithotModulo" 함수는 주어진 숫자가 홀수인지를 반환합니다.

Note
It does so without using the modulo operator(%)
나머지 연산자(%)를 사용하지 마세요
It should work for negative numbers and zero
0과 음수가 주어져도 동작해야 합니다.
*/
let output = isOddWithotModulo(17);
console.log(output); // ---> true

function isOddWithotModulo(num) {
  // TODO
  return;
}
